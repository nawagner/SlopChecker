# 2026-07-31 — 990991A / Alex (Claude Opus 4.8) — Render the near-duplicate pair

**Issues worked:** #22 (stacked on `alex/22-budget-math`).

## What changed

- `scripts/synth/synth_proposals.py` — `add_near_duplicates` now paraphrases at
  the **section level** (was: sections copied verbatim, only the `text` blob
  laundered). The clone's `text` is reassembled from the laundered sections via
  `_assemble`, so the clone is a near-duplicate however it's rendered.
- `scripts/synth/render_fixtures.py` — renders one near-duplicate **pair**
  (`*__near_duplicate_original.*` + `*__near_duplicate_clone.*`) so the similarity
  case lives on disk, not only in `corpus.jsonl`. `files_index.csv` gains a
  `duplicate_of` column and `is_near_duplicate`.
- Corpus regenerated; `files/` re-rendered to 18 documents × 4 formats.
- README updated.

## Why the section-level fix mattered

Before, the clone shared the original's `sections` dict verbatim — so rendering
the clone from sections produced a byte-identical file, not a near-duplicate.
Laundering the sections makes the rendered pair genuinely *near*: measured
similarity 0.92 on the MD pair, non-identical. (The corpus `text` field was
already laundered, so this only affected the rendered output and section-level
consumers.)

## Verified

Pair renders in all four formats; `blog_post__near_duplicate_original` vs
`_clone` similarity 0.92, `identical=False`. ruff clean.

## #22 status

Closes the last polish item. Every fixture case + the near-duplicate pair are now
committed as files. Nothing left on the corpus; #22 awaits merge of the stack
(#48 → #54 → #68 → #73 → #77 → #84 → this).
