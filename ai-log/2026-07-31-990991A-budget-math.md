# 2026-07-31 — 990991A / Alex (Claude Opus 4.8) — Budget arithmetic-error case

**Issues worked:** #22 (stacked on `alex/22-near-dups`). Last corpus item.

## What changed

- `scripts/synth/synth_proposals.py` — grant budgets are now **itemized**
  (personnel / equipment / travel / indirect + a stated total). New grant-only
  slop defect `budget_math`: the stated total does not equal the sum of the line
  items. New ground-truth `budget_math` column. `_extract_budget` now targets the
  labeled "Total requested support is $X" so `requested_budget_usd` is the stated
  total, not a line item.
- `scripts/synth/score.py` — scores `budget_math` with a **label-aware**
  deterministic check (stated total vs. sum of labeled items; robust to the
  sentence-shuffle in laundered docs). Demo: precision/recall 1.00.
- `render_fixtures.py` — adds a `budget_math` fixture (now 16 × 4 files).
- Corpus regenerated (126); README updated; #22 fixture cases marked complete.

## Decision / dead end

- **First pass used positional parsing** (last `$` = total) and got precision
  0.57: `ai_laundered` docs shuffle sentence order, so the total wasn't last.
  Switched to label-aware parsing (`Total requested support is $X` vs. the
  `personnel/equipment/travel/indirect` items) — precision back to 1.00. Real
  budget checks should be label-aware, so this is also the more faithful demo.

## The corpus story is now complete

Naive-baseline recall by defect makes the point cleanly:
- Deterministic (caught): `has_fabricated_citations` 1.00, `budget_math` 1.00,
  `missing_methods` 1.00.
- Needs external data / cross-doc (missed): `has_mismatched_citations` 0.00
  (wrong paper — needs resolution + metadata), `is_near_duplicate` 0.00 (needs
  corpus-wide similarity).

## #22 status

All fixture cases from the ticket are covered. No corpus items remain open.
