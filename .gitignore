# Secrets — never commit
.env
.env.*
!.env.example

# per-machine transcript-upload preference (written by the session after asking)
.slopcheck-transcript

# Python
__pycache__/
*.py[cod]
*.pyc
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Run artifacts
reports/
slopcheck-reports/
*.slopcheck.json
harness/out/

# macOS
.DS_Store

# Worker
worker/node_modules/
worker/.wrangler/

# Agent worktrees — isolated checkouts some harnesses create for subagents.
# A full nested clone of this repo; never belongs in a commit.
.claude/worktrees/
